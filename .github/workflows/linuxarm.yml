name: Build palera1n (Linux ARMEL)

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build-linux-armel:
    runs-on: ubuntu-latest

    env:
      PREFIX: /usr/local
      LIBPLIST_COMMIT: 2117b8fdb6b4096455bd2041a63e59a028120136
      LIBIMOBILEDEVICE_GLUE_COMMIT: 362f7848ac89b74d9dd113b38b51ecb601f76094
      LIBIRECOVERY_COMMIT: 7ce02c347b7c26e59498e6af31c9da51018d0fa1
      LIBIMOBILEDEVICE_COMMIT: ed0d66d0341562731bb19928dfe48155509fa7a7
      LIBUSBMUXD_COMMIT: a7f0543fb1ecb20ac7121c0fd77297200e0e43fc

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      # ----------------------------------------------------
      # Install dependencies
      # ----------------------------------------------------
      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            pkg-config autoconf automake autopoint libtool \
            build-essential mandoc libusb-1.0-0-dev libusb-dev \
            libreadline-dev
          sudo pip3 install jsonschema jinja2

      # ----------------------------------------------------
      # Define retry()
      # ----------------------------------------------------
      - name: Create retry()
        run: |
          echo 'retry() { for i in {1..5}; do "$@" && break || sleep 5; done; }' > retry.sh

      # ----------------------------------------------------
      # Download toolchain
      # ----------------------------------------------------
      - name: Download ARMEL toolchain
        shell: bash
        run: |
          source retry.sh
          retry curl -L -o dep_root_armel.tgz \
            https://cdn.nickchan.lol/palera1n/c-rewrite/releases/v2.2.1/build_depends/dep_root_armel-linux-musleabi.tgz

      - name: Extract toolchain
        run: |
          mkdir -p dep_root
          tar -xzf dep_root_armel.tgz -C dep_root

      # ----------------------------------------------------
      # Configure cross environment
      # ----------------------------------------------------
      - name: Setup cross environment
        run: |
          echo "TOOLCHAIN_DIR=$(pwd)/dep_root" >> $GITHUB_ENV
          echo "$(pwd)/dep_root/bin" >> $GITHUB_PATH
          echo "CROSS_COMPILE=armel-linux-musleabi-" >> $GITHUB_ENV
          echo "DESTDIR=$(pwd)/sysroot" >> $GITHUB_ENV
          echo "PKG_CONFIG_PATH=$(pwd)/sysroot/usr/local/lib/pkgconfig:$(pwd)/dep_root/usr/local/lib/pkgconfig" >> $GITHUB_ENV
          echo "CFLAGS=-I$(pwd)/dep_root/usr/local/include -I$(pwd)/sysroot/usr/local/include" >> $GITHUB_ENV
          echo "LDFLAGS=-L$(pwd)/dep_root/usr/local/lib -L$(pwd)/sysroot/usr/local/lib" >> $GITHUB_ENV

      # ----------------------------------------------------
      # Helper to patch .la libdir paths
      # ----------------------------------------------------
      - name: Create patch-la script
        run: |
          echo 'patch_la() { sed -i "s|libdir='\''/usr/local/lib'\''|libdir='\''$DESTDIR/usr/local/lib'\''|" "$DESTDIR"/usr/local/lib/*.la; }' > patch_la.sh

      # ----------------------------------------------------
      # Build libplist
      # ----------------------------------------------------
      - name: Build libplist
        shell: bash
        run: |
          source retry.sh
          retry git clone https://github.com/libimobiledevice/libplist
          cd libplist
          git reset --hard $LIBPLIST_COMMIT
          autoreconf -fi
          ./configure \
            --host=armel-linux-musleabi \
            --prefix=/usr/local \
            --disable-shared \
            --enable-static
          make -j$(nproc)
          make install DESTDIR="$DESTDIR"
          bash ../patch_la.sh

      # ----------------------------------------------------
      # Build libimobiledevice-glue
      # ----------------------------------------------------
      - name: Build libimobiledevice-glue
        shell: bash
        run: |
          source retry.sh
          retry git clone https://github.com/libimobiledevice/libimobiledevice-glue
          cd libimobiledevice-glue
          git reset --hard $LIBIMOBILEDEVICE_GLUE_COMMIT
          autoreconf -fi

          export CFLAGS="$CFLAGS -I$DESTDIR/usr/local/include"
          export LDFLAGS="$LDFLAGS -L$DESTDIR/usr/local/lib"

          ./configure \
            --host=armel-linux-musleabi \
            --prefix=/usr/local \
            --disable-shared \
            --enable-static

          make -j$(nproc)
          make install DESTDIR="$DESTDIR"
          bash ../patch_la.sh

      # ----------------------------------------------------
      # Build libirecovery (libusb fix)
      # ----------------------------------------------------
      - name: Build libirecovery
        shell: bash
        run: |
          source retry.sh
          retry git clone https://github.com/libimobiledevice/libirecovery
          cd libirecovery
          git reset --hard $LIBIRECOVERY_COMMIT
          autoreconf -fi

          export libusb_CFLAGS="-I/usr/include/libusb-1.0"
          export libusb_LIBS="-lusb-1.0"
          export CFLAGS="$CFLAGS -I/usr/include/libusb-1.0"
          export LDFLAGS="$LDFLAGS -lusb-1.0"

          ./configure \
            --host=armel-linux-musleabi \
            --prefix=/usr/local \
            --disable-shared \
            --enable-static

          make -j$(nproc)
          make install DESTDIR="$DESTDIR"
          bash ../patch_la.sh

      # ----------------------------------------------------
      # Build libimobiledevice
      # ----------------------------------------------------
      - name: Build libimobiledevice
        shell: bash
        run: |
          source retry.sh
          retry git clone https://github.com/libimobiledevice/libimobiledevice
          cd libimobiledevice
          git reset --hard $LIBIMOBILEDEVICE_COMMIT
          autoreconf -fi

          export CFLAGS="$CFLAGS -I$DESTDIR/usr/local/include"
          export LDFLAGS="$LDFLAGS -L$DESTDIR/usr/local/lib"

          ./configure \
            --host=armel-linux-musleabi \
            --prefix=/usr/local \
            --disable-shared \
            --enable-static \
            --with-mbedtls \
            --disable-debug \
            --disable-wireless-pairing

          make -j$(nproc)
          make install DESTDIR="$DESTDIR"
          bash ../patch_la.sh

      # ----------------------------------------------------
      # Build libusbmuxd
      # ----------------------------------------------------
      - name: Build libusbmuxd
        shell: bash
        run: |
          source retry.sh
          retry git clone https://github.com/libimobiledevice/libusbmuxd
          cd libusbmuxd
          git reset --hard $LIBUSBMUXD_COMMIT
          autoreconf -fi

          export CFLAGS="$CFLAGS -I$DESTDIR/usr/local/include"
          export LDFLAGS="$LDFLAGS -L$DESTDIR/usr/local/lib"

          ./configure \
            --host=armel-linux-musleabi \
            --prefix=/usr/local \
            --disable-shared \
            --enable-static

          make -j$(nproc)
          make install DESTDIR="$DESTDIR"
          bash ../patch_la.sh

      # ----------------------------------------------------
      # Merge sysroot -> toolchain
      # ----------------------------------------------------
      - name: Merge sysroot into dep_root
        run: |
          cp -a sysroot/usr/local/include/* dep_root/usr/local/include/ || true
          cp -a sysroot/usr/local/lib/* dep_root/usr/local/lib/ || true

      # ----------------------------------------------------
      # Build palera1n
      # ----------------------------------------------------
      - name: Build palera1n (linux-armel)
        shell: bash
        run: |
          make clean || true
          make linux-armel

      # ----------------------------------------------------
      # Upload final binary
      # ----------------------------------------------------
      - name: Upload palera1n artifact
        uses: actions/upload-artifact@v4
        with:
          name: palera1n-linux-armel
          path: palera1n
